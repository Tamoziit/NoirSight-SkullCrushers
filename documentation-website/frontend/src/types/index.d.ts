export interface ProjectProps {
    company: string;
    projectName: string;
}

export interface ApiKeyData {
  apiKey: string;
  company: string;
  projectName: string;
  userId: string;
  validity: string;
}